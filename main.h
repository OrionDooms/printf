#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>
#include <stdarg.h>
#include <unistd.h>
#include <string.h>

int _putchar(char c);
int _printf(const char *format, ...);
int _puts(char *s);
int print_int(int n);
void print_num(int n);
int count_num(int n);
void binary_num(int n);
int binary_int(int n);

#endif
